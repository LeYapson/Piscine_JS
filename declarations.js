const escapeStr = "\`,\\,/,\",'"
console.log(escapeStr)

const arr =[4, "2"]
console.log(arr)

const obj = {
    str : "hello",

    num : 42,

    bool : true,

    undef : undefined
}

const nested = {
    arr : [4, undefined, '2'],
    obj : {
        str : "hello",
        num : 42,
        bool : true
    }
}
