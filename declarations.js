const escapestr = '/\'\"\`\\'
console.log(escapestr)

const arr =[4, '2']
console.log(arr)

const obj = {
    str : "hello",

    num : 42,

    bool : true,

    undef : undefined
}

const nested = {
    arr : [3, undefined, '2'],
    obj : {
        str : "hello",
        num : 42,
        bool : true
    }
}
